# MySQL (used by Prisma)
DATABASE_URL="mysql://root:password@db:3306/jobsdb"

# Webhook.site URL (set to https://webhook.site/<your-id>)
WEBHOOK_SITE_URL="https://webhook.site/<YOUR_ID>"

# API Settings
API_PORT=4000

# MySQL root password for Docker
MYSQL_ROOT_PASSWORD=password
MYSQL_DATABASE=jobsdb
MYSQL_USER=root
MYSQL_PASSWORD=password
